# DanScribe local config & logs (never commit — may contain an API key)
.danscribe_config.json
.danscribe.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# PyInstaller build artifacts
build/
dist/
*.spec

# Built executables. DanScribe_v3.exe is ~330 MB and was sitting untracked next
# to the source; one `git add -A` would have committed it permanently.
*.exe

# OS cruft
.DS_Store
Thumbs.db
